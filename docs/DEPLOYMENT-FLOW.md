# 🔄 CI/CD Deployment Flow Diagram

## 📊 Complete Workflow Visualization

```
┌─────────────────────────────────────────────────────────────────┐
│                     GITHUB ACTIONS WORKFLOW                      │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│  Code Push   │  (dev/staging/main branch)
│  or PR       │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  STAGE 1: VALIDATE                                               │
│  ✓ Checkout code                                                 │
│  ✓ Setup Terraform                                               │
│  ✓ Format check (terraform fmt)                                  │
│  ✓ Initialize (terraform init)                                   │
│  ✓ Validate syntax (terraform validate)                          │
└──────┬───────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  STAGE 2: SECURITY SCAN                                          │
│  🔒 Run Checkov security scanner                                 │
│  🔒 Check for vulnerabilities                                    │
│  🔒 Verify best practices                                        │
└──────┬───────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  STAGE 3: PLAN                                                   │
│  📋 Configure AWS credentials                                    │
│  📋 Select workspace (dev/staging/prod)                          │
│  📋 Run terraform plan                                           │
│  📋 Upload plan artifact                                         │
│  📋 Comment on PR (if applicable)                                │
└──────┬───────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  STAGE 4: DEPLOY (main branch or manual only)                   │
│  🚀 Configure AWS credentials                                    │
│  🚀 Select workspace                                             │
│  🚀 Run terraform apply                                          │
│  🚀 Capture outputs (URLs, IDs)                                  │
│  🚀 Generate deployment summary                                  │
└──────┬───────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  DEPLOYMENT COMPLETE ✅                                          │
│  • ALB URL available                                             │
│  • CloudFront URL available                                      │
│  • S3 bucket created                                             │
│  • All services running                                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ AWS Resource Creation Order

```
┌─────────────────────────────────────────────────────────────────┐
│                    TERRAFORM DEPLOYMENT ORDER                    │
└─────────────────────────────────────────────────────────────────┘

LAYER 1: FOUNDATION
┌──────────────┐
│     VPC      │  Creates: VPC, Subnets, Internet Gateway, Route Tables
└──────┬───────┘
       │
       ▼
LAYER 2: SECURITY
┌──────────────────┐
│ Security Groups  │  Creates: Firewall rules for EC2, RDS, Lambda
└──────┬───────────┘
       │
       ▼
LAYER 3: STORAGE & IDENTITY
┌──────────┐     ┌──────────┐
│    S3    │     │   IAM    │  Creates: S3 bucket, IAM roles & policies
└──────┬───┘     └────┬─────┘
       │              │
       └──────┬───────┘
              ▼
LAYER 4: COMPUTE & DATABASE
┌──────────┐     ┌──────────┐
│   EC2    │     │   RDS    │  Creates: EC2 instances, RDS database
└──────┬───┘     └────┬─────┘
       │              │
       └──────┬───────┘
              ▼
LAYER 5: SCALING & MONITORING
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  CloudWatch  │  │ Auto Scaling │  │     ALB      │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │
       └─────────────────┼─────────────────┘
                         ▼
LAYER 6: CDN, DNS & SERVERLESS
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  CloudFront  │  │   Route53    │  │    Lambda    │
└──────────────┘  └──────────────┘  └──────────────┘
```

---

## 🔀 Branch Strategy Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      GIT BRANCH WORKFLOW                         │
└─────────────────────────────────────────────────────────────────┘

feature/new-service
       │
       │ (PR + Review)
       ▼
     dev ──────────► Validate → Scan → Plan → Deploy to DEV
       │
       │ (Merge after testing)
       ▼
   staging ────────► Validate → Scan → Plan → Deploy to STAGING
       │
       │ (Merge after approval)
       ▼
     main ─────────► Validate → Scan → Plan → Deploy to PROD
                                                (with approval)
```

---

## 🎯 Trigger Types

```
┌─────────────────────────────────────────────────────────────────┐
│                      WORKFLOW TRIGGERS                           │
└─────────────────────────────────────────────────────────────────┘

1. PUSH EVENT
   git push origin dev/staging/main
   └─► Runs: Validate → Scan → Plan → Deploy

2. PULL REQUEST
   Create PR to main
   └─► Runs: Validate → Scan → Plan (no deploy)
       └─► Comments plan on PR

3. MANUAL TRIGGER
   Actions → Run workflow → Select environment
   └─► Runs: Validate → Scan → Plan → Deploy
       └─► Full control over environment

4. DESTROY (Manual Only)
   Actions → Run workflow → Destroy job
   └─► Requires approval
       └─► Destroys all infrastructure
```

---

## 📦 Artifact Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      ARTIFACT MANAGEMENT                         │
└─────────────────────────────────────────────────────────────────┘

Plan Stage
    │
    ├─► Generate tfplan file
    │
    ├─► Upload as artifact
    │   └─► Retention: 5 days
    │
    └─► Available for:
        • Manual review
        • Audit trail
        • Rollback reference
```

---

## 🔐 Secrets & Variables Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                   SECRETS & VARIABLES USAGE                      │
└─────────────────────────────────────────────────────────────────┘

GitHub Secrets
    │
    ├─► AWS_ACCESS_KEY_ID ────────┐
    ├─► AWS_SECRET_ACCESS_KEY ────┤
    ├─► DB_PASSWORD ──────────────┤
    └─► ALARM_EMAIL ──────────────┤
                                   │
                                   ▼
                          Injected into Workflow
                                   │
                                   ▼
                          Used by Terraform
                                   │
                                   ▼
                          Creates AWS Resources
```

---

## 🚦 Environment Protection

```
┌─────────────────────────────────────────────────────────────────┐
│                    ENVIRONMENT PROTECTION RULES                  │
└─────────────────────────────────────────────────────────────────┘

DEV Environment
└─► No protection
    └─► Auto-deploy ✅

STAGING Environment
└─► Optional reviewers
    └─► Auto-deploy ✅

PROD Environment
└─► Required reviewers (1-2) ⚠️
    └─► Wait timer: 5 min ⏱️
        └─► Deploy after approval ✅

DESTROY Environment
└─► Required reviewers (2+) 🚨
    └─► Manual approval only ⚠️
        └─► Destroys infrastructure 🗑️
```

---

## 📊 Success Metrics

```
┌─────────────────────────────────────────────────────────────────┐
│                      DEPLOYMENT METRICS                          │
└─────────────────────────────────────────────────────────────────┘

Validate Stage:     ~1-2 minutes
Security Scan:      ~2-3 minutes
Plan Stage:         ~3-5 minutes
Deploy Stage:       ~10-15 minutes
─────────────────────────────────────
Total Time:         ~16-25 minutes

Resources Created:
• 1 VPC
• 2 Subnets
• 1-3 EC2 instances
• 1 RDS database
• 1 S3 bucket
• 1 ALB
• 1 CloudFront distribution
• 1 Lambda function
• Multiple Security Groups
• CloudWatch alarms
• Route53 records
```

---

## 🎨 Status Indicators

```
✅ Success    - All checks passed, deployment complete
❌ Failed     - Error occurred, check logs
🟡 Running    - Workflow in progress
⏸️  Waiting   - Pending approval
🔄 Queued     - Waiting for runner
⏭️  Skipped   - Condition not met
```

---

**Quick Reference Guide**  
**Version**: 1.0  
**Last Updated**: 2024
